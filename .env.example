# Model Selection Configuration
# Choose which AI model to use for trading decisions
# Examples:
#   MODEL=gpt-4o                    (OpenAI GPT-4o - default)
#   MODEL=gpt-4o-mini              (OpenAI GPT-4o-mini - cheaper)
#   MODEL=gemini-2.5-pro           (Google Gemini 2.5 Pro)
#   MODEL=gemini-2.5-flash         (Google Gemini 2.5 Flash - faster)
#   MODEL=gemini-1.5-flash         (Google Gemini 1.5 Flash)
MODEL=gpt-4o

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Set a custom base URL for OpenAI API (leave blank for default)
# OPENAI_BASE_URL=

# Optional: Set organization ID if you have one
# OPENAI_ORG_ID=your-org-id-here

# Google Gemini API Configuration (Required if using Gemini models)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Alpaca Trading API Configuration
# Get your paper trading keys from: https://app.alpaca.markets/paper/dashboard/overview
ALPACA_API_KEY=your-paper-trading-key-id
ALPACA_SECRET_KEY=your-paper-trading-secret-key

# Alpaca API Base URL for paper trading (do not change)
# Note: The Alpaca library adds /v2 automatically, so don't include it in the base URL
ALPACA_BASE_URL=https://paper-api.alpaca.markets

# Brave Search API Configuration (Optional - for real-time news and market research)
# Get your free API key from: https://api.search.brave.com/
# Free tier: 2,000 searches per month, 1 request per second
BRAVE_API_KEY=your-brave-search-api-key-here

# AI Model Caching Configuration
# Prompt caching reduces costs and improves response times by reusing system prompts

# Cache TTL (Time To Live) in seconds for Gemini explicit caching
# Default: 3600 seconds (1 hour) - balance between cost savings and cache freshness
# Range: 60 to 7200 seconds (1 minute to 2 hours recommended)
CACHE_TTL_SECONDS=3600

# Enable/disable explicit caching for Gemini models
# Set to "false" to disable explicit caching (implicit caching still works)
# OpenAI models use automatic caching (cannot be disabled)
ENABLE_EXPLICIT_CACHING=true